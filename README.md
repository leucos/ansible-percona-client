ansible-percona-client
======================

Installs percona client libraries (debian only)

## Defaults

`percona_client_version`: `percona_version` or 5.6 if not set (see
https://github.com/leucos/ansible-percona).



